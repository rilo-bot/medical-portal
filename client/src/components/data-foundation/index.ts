/**
 * data-foundation barrel export
 *
 * Exposes utilities built in this slice for use by page components.
 * All store APIs are consumed via their own named hooks — this module
 * only re-exports supplemental helpers.
 */

export { useProcessingPolling } from './useProcessingPolling'
